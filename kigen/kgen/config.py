from kigen.config import temp_root

# not needed but for consistency
temp = {'root' :        temp_root + '/kgen' ,             \
        'work' :        temp_root + '/kgen' + '/work',    \
        'cache':        temp_root + '/kgen' + '/cache',   \
        'distfiles':    temp_root + '/kgen' + '/distfiles'}

