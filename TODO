NODO: rewrite add_options method in kernel to use kernel script /usr/src/linux/scripts/config
    why not? now that I have that patched in LKML
NODO: make --fakemod=/ in case kernel modules are copied from another chroot
    kigen i --fakemod=/chroot can feed from kigen k --fakeroot=/chroot
NODO: make multiple targets callable (left for the next new whole rewrite of cliparser.py)
    do kigen k && kigen i instead

0.3.1
=====
TODO: migrate bin code from append.py to initramfs/bin
TODO: fix bin_luks sbin/bin thingy
TODO: --source-screen is actually not static :( fix it
    but LDFLAGS=-static ./configure fails with missing getent :(
TODO: add timestamp for each line in logfile and beautify logs a bit
TODO: move all bin_feat logic to its own dir and create binary classes like with sources.
    -> initramfs.append.source.luks
    -> initramfs.append.bin.luks
TODO: check uuid feature is really working
TODO: take some time and clean append.py, for real
TODO: use presets from modules/default.py if none are found in /etc/kigen/*

0.3.*
=====
TODO: try to display modules list by category instead of just listing them
    - crypt fs net ataraid ... see MY_HWOPTS
TODO: remove that 'root block device() ::' loop in linuxrc and drop to shell instead
    - to help user provide sortof message that points boot scripts in his current dir
    - boot.sh boot-luk.sh boot-luks-lvm2.sh boot-dmraid.sh...
TODO: redesign kigen i -h/usage.py with a possible ascii array structure, gotta dig first
TODO: add USE=module-rebuild and option to /etc/kigen/ and module-rebuild dep in ebuild
TODO: add <pkg>_hash= to /etc/kigen/initramfs/hash.conf
    add --nohash option (as in Misc:)
TODO: apply mdstart.sh patch applet for busybox (GK-3.4.11)
    what for already?
TODO: dont fail when finding a dynamically linked binary
    - raise a warning if the user passed --plugin maybe he knows what he's doing
    - let ldd parse a dynamically linked binary and get a list of libs to ship
    already thrown some code at it but it gets quickly messy (will do when sober)
    this feat is awesome actually: it raises a lot the probability of having a fine first build
    regardless of static/dyn binaries
TODO: kigen k --fixdotconfig=selinux,initramfs,foo,bar
    http://www.gentoo.org/proj/en/hardened/selinux/selinux-handbook.xml?part=2&chap=2
    http://www.gentoo.org/proj/en/hardened/pax-quickstart.xml
TODO: find a way to split append.py a bit more
    dissociate a binary class too? why not?
    split append.py into append_feature.py? append_luks.py ...
    append directory?
TODO: offload append.py to initramfs.py wrt bin/source checks
TODO: kigen i --screen
    see if screen can attach to /dev/console
    I need something clean to replace ttyecho 
TODO: kigen i --gnupg
    gnupg-version = 2.0.15
    cp ~/key.gpg /usr/src/initramfs/root/
TODO: kigen i --aufs
    aufs-version = 0_p20100405
TODO: support xen-sources -> doublehp this one is for you ;) patience though
    lucky bastard linux-3.0 will have full Xen support
    start dev with gentoo-sources-3.0 or fall back to xen-sources
    http://en.gentoo-wiki.com/wiki/Xen
    read dracut code there lies the solution to xen and other stuff
